/**
 * This is a kingdee cosmic template project that is automatically generated by the Kingdee cosmic development assistant plugin. 
 * If there are any issues during the use process, you can provide feedback to the kingdee developer community website.
 * Website: https://developer.kingdee.com/developer?productLineId=29
 * Author: liebin.zheng
 * Generate Date: 2024-11-23 09:45:05
 */
rootProject.name = System.getProperty('artifactId')

include(
		'akmv-cosmic-debug',
		':akmv-base-common',
		':akmv-base-helper',
		
		':akmv-fi-fic',

		':akmv-esc-esc',

		':akmv-tmc-tmc',

		':akmv-scmc-scmc',

		':akmv-pur-pur',

		':akmv-qmc-qmc',

		':akmv-mpscmm-mpscmm',

		':akmv-bamp-bamp',

		':akmv-mmc-mmc',

		':akmv-wfs-wfs',

		':kd',
)

// -------------- 引入苍穹调试工程模块 --------------
//project(':akmv-cosmic-debug').projectDir = new File('akmv-cosmic-debug')

// -------------- 引入公共基础模块 --------------
project(':akmv-base-common').projectDir = new File('base/akmv-base-common')
project(':akmv-base-helper').projectDir = new File('base/akmv-base-helper')

// -------------- 引入fi云-fic应用模块 --------------
project(':akmv-fi-fic').projectDir = new File('fi/akmv-fi-fic')

// -------------- 引入esc云-esc应用模块 --------------
project(':akmv-esc-esc').projectDir = new File('esc/akmv-esc-esc')

// -------------- 引入tmc云-tmc应用模块 --------------
project(':akmv-tmc-tmc').projectDir = new File('tmc/akmv-tmc-tmc')

// -------------- 引入scmc云-scmc应用模块 --------------
project(':akmv-scmc-scmc').projectDir = new File('scmc/akmv-scmc-scmc')

// -------------- 引入pur云-pur应用模块 --------------
project(':akmv-pur-pur').projectDir = new File('pur/akmv-pur-pur')

// -------------- 引入qmc云-qmc应用模块 --------------
project(':akmv-qmc-qmc').projectDir = new File('qmc/akmv-qmc-qmc')

// -------------- 引入mpscmm云-mpscmm应用模块 --------------
project(':akmv-mpscmm-mpscmm').projectDir = new File('mpscmm/akmv-mpscmm-mpscmm')

// -------------- 引入bamp云-bamp应用模块 --------------
project(':akmv-bamp-bamp').projectDir = new File('bamp/akmv-bamp-bamp')

// -------------- 引入mmc云-mmc应用模块 --------------
project(':akmv-mmc-mmc').projectDir = new File('mmc/akmv-mmc-mmc')

// -------------- 引入wfs云-wfs应用模块 --------------
project(':akmv-wfs-wfs').projectDir = new File('wfs/akmv-wfs-wfs')

// -------------- 引入前期项目唐廷应用模块 --------------
project(':kd').projectDir = new File('pre/kd')