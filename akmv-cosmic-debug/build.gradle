/*
 * This is a kingdee cosmic template project that is automatically generated by the Kingdee cosmic development assistant plugin. 
 * If there are any issues during the use process, you can provide feedback to the kingdee developer community website.
 * Website: https://developer.kingdee.com/developer?productLineId=29
 * Author: liebin.zheng
 * Generate Date: 2024-11-23 09:45:05
 */

dependencies {

	// 公共基础包
	implementation project(':akmv-base-common')
	implementation project(':akmv-base-helper')

	// 引入fi云 - fic应用模块
	implementation project(':akmv-fi-fic')
	
	// 引入esc云 esc应用模块
	implementation project(':akmv-esc-esc')

	// 引入tmc云 tmc应用模块
	implementation project(':akmv-tmc-tmc')

	// 引入scmc云 scmc应用模块
	implementation project(':akmv-scmc-scmc')

	// 引入pur云 pur应用模块
	implementation project(':akmv-pur-pur')

	// 引入qmc云 qmc应用模块
	implementation project(':akmv-qmc-qmc')

	// 引入mpscmm云 mpscmm应用模块
	implementation project(':akmv-mpscmm-mpscmm')

	// 引入bamp云 bamp应用模块
	implementation project(':akmv-bamp-bamp')

	// 引入mmc云 mmc应用模块
	implementation project(':akmv-mmc-mmc')

	// 引入wfs云 wfs应用模块
	implementation project(':akmv-wfs-wfs')

}